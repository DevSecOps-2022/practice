- name: checking vars presedence
  hosts: local
  connection: local
  vars:
   COURSE: devops
   TRAINER: siva
   DURATION: "5 months"
  tasks: 
  - debug:
     msg: "learning{{COURSE}}, trainer is  {{TRAINER}} and the time duration is {{DURATION}}"
